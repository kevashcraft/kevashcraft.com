{% extends 'templates/master.twig' %}

{% set title = '#' ~ tag %}

{% block content %}
<a href="/tags/{{ tag|lower }}" title="{{ tag }} Tutorials"><h3>#{{ tag }}</h3></a>
<section>
  <ul>
    {% for tutorial in tutorials %}
    <li>
      <a href="/{{ tutorial.folder }}/{{ tutorial.page }}" title="{{ tutorial.title }}">{{ tutorial.title }}</a>
    </li>
    {% endfor %}
  </ul>
</section>
{% endblock %}
